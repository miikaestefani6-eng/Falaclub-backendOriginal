import { createFileRoute } from "@tanstack/react-router";
import { BarChart3, Bell, BookOpen, CreditCard, GraduationCap, Users } from "lucide-react";
import { AppShell, SectionCard, Stat } from "@/components/app-shell";
import { requireRole } from "@/lib/auth";

export const Route = createFileRoute("/master")({ beforeLoad: async () => ({ profile: await requireRole(["super_admin"]) }), component: Master });
function Master() { return <AppShell titulo="CMS Master" subtitulo="Gestão completa do FalaClub"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Stat rotulo="Usuários" valor="—" /><Stat rotulo="Professores" valor="—" /><Stat rotulo="Escolas" valor="—" /><Stat rotulo="Creators" valor="—" /></div><div className="mt-5 grid gap-5 md:grid-cols-2"><SectionCard titulo="Gestão"><div className="grid gap-2 sm:grid-cols-2">{[[Users,"Usuários"],[GraduationCap,"Professores"],[BookOpen,"Biblioteca"],[CreditCard,"Financeiro"]].map(([Icon,label]) => <div key={label as string} className="flex items-center gap-3 rounded-2xl border border-border p-4"><Icon className="size-5 text-primary" /><span className="text-sm font-semibold">{label as string}</span></div>)}</div></SectionCard><SectionCard titulo="Operação"><div className="grid gap-2 sm:grid-cols-2"><div className="flex items-center gap-3 rounded-2xl border border-border p-4"><BarChart3 className="size-5 text-primary" />Analytics</div><div className="flex items-center gap-3 rounded-2xl border border-border p-4"><Bell className="size-5 text-primary" />Notificações</div></div></SectionCard></div></AppShell>; }
